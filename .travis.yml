sudo: required

language: java

services:
  - docker

before_install:
  - docker build -t riaan/naair .
  - docker ps -a
  - docker run riaan/naair /bin/sh -c "cd green/;ant -S; ant test"

script:
  - ant -S
  - ant test
