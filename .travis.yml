sudo: required

language: java

services:
  - docker

before_install:
  - docker build -t riaan/naair .
  - docker ps -a
  - docker run riaan/naair /bin/sh -c "ant -S; ant test"

script:
  - ant -S
  - ant test
