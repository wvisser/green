sudo: required

language: java

services:
    - docker

before_install:
    - docker build -t hen3kr8/green .
    - docker run -d -p 127.0.0.1:8001 hen3kr8/green
    - docker ps -a
    - docker run hen3kr8/green

script:
    - ant;
    - ant test;
