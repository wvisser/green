sudo: required
dist: trusty

services: docker

before_script:
- sudo rm /usr/local/bin/docker-compose  
- chmod +x docker-compose
- sudo mv docker-compose /usr/local/bin

language: java

script:
 - ant;
 - ant test;

after_script:
 - docker-compose down
