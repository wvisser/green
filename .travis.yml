sudo: required

language: java

services:
	- docker

before_install:
	- docker build -t riaan/naair .
	- docker ps -a
	- docker run riaan/naair /bin/sh -c "cd /root/naair; bundle exec rake test"

script:
	- ant;
	- ant test;
