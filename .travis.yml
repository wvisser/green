sudo: required

language: java

services:
  - docker

before_install:
  - docker build -t riaan/naair .
  - docker ps -a
  - docker run riaan/naair /bin/sh -c "ant; ant test"

script:
  - ant
  - ant test
