sudo: required

language: java

services:
  - docker

before_install:
  - docker build -t green .

script:
  - docker container logs -f green
  - docker run green /bin/sh -c "ant; ant test;"
  - docker ps -a
