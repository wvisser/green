sudo: required

services:
 - docker

language: java

before_install:
 - docker build -t green .
 - docker run -d -p 127.0.0.1:80:4567 green /bin/sh -c "cd /root/green; ant;"
 - docker ps -a
 - docker run green /bin/sh -c "cd /root/green; ant test"

script:
 - ant;
 - ant test;
