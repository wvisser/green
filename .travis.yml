sudo: required

language: java

services:
  - docker

before_install:
  - docker build -t green .

script:
  # - docker run green -name cont /bin/sh -c "ant; ant test;"
  - docker run -d --name cont green /bin/sh -c "ant; ant test -v"
  - docker logs --f cont
  - docker logs --details cont
